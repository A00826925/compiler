program MyProgram;
var 
  number1: int;
  number2: float;
  x: int;
  y: int;

void simplefunc(floaty: float, inty:int)[{}];
main
{ 
  x = 10 + 5;
  y = 5.5;
  if (x > 5) {
    print("x is greater than 5");
  } else {
    print("x is not greater than 5");
  };
  while {x = -5+6;} do (
    x > 5
  );
}
end
